# Git hook'ları — anti-stack defteri kararı: husky yerine lefthook (HEDEF_MIMARI-8 §11)
pre-commit:
  commands:
    biome:
      glob: "*.{ts,tsx,mjs,json}"
      run: npx biome check --no-errors-on-unmatched {staged_files}

pre-push:
  commands:
    test:
      run: npx vitest run
